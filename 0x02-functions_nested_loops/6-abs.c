#include "main.h"
#include <stdlib.h>

/**
 * _abs - function takes an integer as argument and returns its absolute value
 * @i: is the a-integer argument
 *
 * Return: the absolute value of the argument
 */
int _abs(int i)
{
	int c;

	c = abs(i);
	return (c);
}
